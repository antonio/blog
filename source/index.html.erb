<ul>
  <% blog.articles.each do |article| %>
    <li>
      <time datatetime="<%= article.date %>" class="article-date"><%= article.date.strftime("%Y %b %d") %></time>
      <%= link_to(article.title, article.url, class: 'article-title') %>
  <% end %>
</ul>
